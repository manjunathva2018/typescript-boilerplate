run npm install to install the required dependencies

> npm install

To compile the files and run the code inside a terminal through node, open 2 command prompts and type the below commands.

> npm run watch
> npm run start

To compile and run through the browser , open a command prompt and run the below command.

> npm run open
